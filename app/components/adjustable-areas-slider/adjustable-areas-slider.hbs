{{#if _areas}}
	{{#each _areas}}
		<div class="area" {{bind-attr style="style"}}>{{name}}</div>
	{{/each}}
	{{#if sliderBeingDragged}}
		<div class="mask"></div>
	{{/if}}
	{{view SlidersView}}
{{else}}
	Add stories to adjust time
{{/if}}